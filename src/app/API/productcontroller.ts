import { BaseURL } from '../config';

export const productcontroller=
 {
    GetAllPosts: BaseURL + `Posts.json`,
    GetAllUsers:BaseURL+ 'users.json',
    GetAllProduct: BaseURL+'product.json',
    
}